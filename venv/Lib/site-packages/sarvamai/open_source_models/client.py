# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..requests.chat_completion_request_message import ChatCompletionRequestMessageParams
from ..requests.chat_completion_tool import ChatCompletionToolParams
from ..requests.response_format import ResponseFormatParams
from ..requests.stop_configuration import StopConfigurationParams
from ..requests.tool_choice_option import ToolChoiceOptionParams
from ..types.create_chat_completion_response import CreateChatCompletionResponse
from ..types.model_list_response import ModelListResponse
from ..types.model_object import ModelObject
from ..types.reasoning_effort import ReasoningEffort
from ..types.v2model_ids import V2ModelIds
from .raw_client import AsyncRawOpenSourceModelsClient, RawOpenSourceModelsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class OpenSourceModelsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawOpenSourceModelsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawOpenSourceModelsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawOpenSourceModelsClient
        """
        return self._raw_client

    def chat_completions_v2(
        self,
        *,
        messages: typing.Sequence[ChatCompletionRequestMessageParams],
        model: V2ModelIds,
        temperature: typing.Optional[float] = OMIT,
        top_p: typing.Optional[float] = OMIT,
        reasoning_effort: typing.Optional[ReasoningEffort] = OMIT,
        max_tokens: typing.Optional[int] = OMIT,
        stream: typing.Optional[bool] = OMIT,
        stop: typing.Optional[StopConfigurationParams] = OMIT,
        n: typing.Optional[int] = OMIT,
        seed: typing.Optional[int] = OMIT,
        frequency_penalty: typing.Optional[float] = OMIT,
        presence_penalty: typing.Optional[float] = OMIT,
        tools: typing.Optional[typing.Sequence[ChatCompletionToolParams]] = OMIT,
        tool_choice: typing.Optional[ToolChoiceOptionParams] = OMIT,
        response_format: typing.Optional[ResponseFormatParams] = OMIT,
        extra_body: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateChatCompletionResponse:
        """
        Creates a model response for the given chat conversation. OpenAI-compatible request and response shape; differences from OpenAI are called out in the Open-Source Models documentation.

        Requires `api-subscription-key`. Keys without beta access to this endpoint receive `400 invalid_request_error` before the model is called. Set `"stream": true` to receive the reply incrementally as server-sent events. Reasoning models return their chain-of-thought in a separate `reasoning_content` field on the message (`delta.reasoning_content` when streaming); reasoning tokens count against `max_tokens`.

        Parameters
        ----------
        messages : typing.Sequence[ChatCompletionRequestMessageParams]
            A list of messages comprising the conversation so far.

        model : V2ModelIds

        temperature : typing.Optional[float]
            What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.
            We generally recommend altering this or `top_p` but not both.

        top_p : typing.Optional[float]
            An alternative to sampling with temperature, called nucleus sampling,
            where the model considers the results of the tokens with top_p probability
            mass. So 0.1 means only the tokens comprising the top 10% probability mass
            are considered.

            We generally recommend altering this or `temperature` but not both.

        reasoning_effort : typing.Optional[ReasoningEffort]
            The effort to use for reasoning

        max_tokens : typing.Optional[int]
            The maximum number of tokens that can be generated in the chat completion.

        stream : typing.Optional[bool]
            If set to true, the model response data will be streamed to the client
            as it is generated using [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format).

        stop : typing.Optional[StopConfigurationParams]

        n : typing.Optional[int]
            How many chat completion choices to generate for each input message. Note that you will be charged based on the number of generated tokens across all of the choices. Keep `n` as `1` to minimize costs.

        seed : typing.Optional[int]
            This feature is in Beta.
            If specified, our system will make a best effort to sample deterministically, such that repeated requests with the same `seed` and parameters should return the same result.
            Determinism is not guaranteed, and you should refer to the `system_fingerprint` response parameter to monitor changes in the backend.

        frequency_penalty : typing.Optional[float]
            Number between -2.0 and 2.0. Positive values penalize new tokens based on
            their existing frequency in the text so far, decreasing the model's
            likelihood to repeat the same line verbatim.

        presence_penalty : typing.Optional[float]
            Number between -2.0 and 2.0. Positive values penalize new tokens based on
            whether they appear in the text so far, increasing the model's likelihood
            to talk about new topics.

        tools : typing.Optional[typing.Sequence[ChatCompletionToolParams]]
            A list of tools the model may call. Currently, only functions are supported as a tool.

        tool_choice : typing.Optional[ToolChoiceOptionParams]
            Controls which (if any) tool is called by the model.

        response_format : typing.Optional[ResponseFormatParams]
            An object specifying the format that the model must output. Setting `{ "type": "json_schema", "json_schema": {...} }` enables Structured Outputs which guarantees the model generates output matching the supplied JSON Schema. Setting `{ "type": "json_object" }` enables the older JSON mode, which guarantees valid JSON but not a specific schema.

        extra_body : typing.Optional[typing.Dict[str, typing.Any]]
            Extra body to be sent to the model.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateChatCompletionResponse
            Successful Response

        Examples
        --------
        from sarvamai import SarvamAI

        client = SarvamAI(
            api_subscription_key="YOUR_API_SUBSCRIPTION_KEY",
        )
        client.open_source_models.chat_completions_v2(
            messages=[
                {
                    "role": "user",
                    "content": "Summarise the causes of the 2008 financial crisis.",
                }
            ],
            model="glm5.2",
        )
        """
        _response = self._raw_client.chat_completions_v2(
            messages=messages,
            model=model,
            temperature=temperature,
            top_p=top_p,
            reasoning_effort=reasoning_effort,
            max_tokens=max_tokens,
            stream=stream,
            stop=stop,
            n=n,
            seed=seed,
            frequency_penalty=frequency_penalty,
            presence_penalty=presence_penalty,
            tools=tools,
            tool_choice=tool_choice,
            response_format=response_format,
            extra_body=extra_body,
            request_options=request_options,
        )
        return _response.data

    def list_models_v2(self, *, request_options: typing.Optional[RequestOptions] = None) -> ModelListResponse:
        """
        Lists the model IDs this deployment currently serves. A model is only served where its backend is configured, so the list is environment-specific — fetch it rather than hardcoding IDs.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ModelListResponse
            Successful Response

        Examples
        --------
        from sarvamai import SarvamAI

        client = SarvamAI(
            api_subscription_key="YOUR_API_SUBSCRIPTION_KEY",
        )
        client.open_source_models.list_models_v2()
        """
        _response = self._raw_client.list_models_v2(request_options=request_options)
        return _response.data

    def get_model_v2(self, model_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> ModelObject:
        """
        Retrieves one served model by ID.

        Parameters
        ----------
        model_id : str
            A model ID from `GET /v2/models`, e.g. `glm5.2`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ModelObject
            Successful Response

        Examples
        --------
        from sarvamai import SarvamAI

        client = SarvamAI(
            api_subscription_key="YOUR_API_SUBSCRIPTION_KEY",
        )
        client.open_source_models.get_model_v2(
            model_id="model_id",
        )
        """
        _response = self._raw_client.get_model_v2(model_id, request_options=request_options)
        return _response.data


class AsyncOpenSourceModelsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawOpenSourceModelsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawOpenSourceModelsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawOpenSourceModelsClient
        """
        return self._raw_client

    async def chat_completions_v2(
        self,
        *,
        messages: typing.Sequence[ChatCompletionRequestMessageParams],
        model: V2ModelIds,
        temperature: typing.Optional[float] = OMIT,
        top_p: typing.Optional[float] = OMIT,
        reasoning_effort: typing.Optional[ReasoningEffort] = OMIT,
        max_tokens: typing.Optional[int] = OMIT,
        stream: typing.Optional[bool] = OMIT,
        stop: typing.Optional[StopConfigurationParams] = OMIT,
        n: typing.Optional[int] = OMIT,
        seed: typing.Optional[int] = OMIT,
        frequency_penalty: typing.Optional[float] = OMIT,
        presence_penalty: typing.Optional[float] = OMIT,
        tools: typing.Optional[typing.Sequence[ChatCompletionToolParams]] = OMIT,
        tool_choice: typing.Optional[ToolChoiceOptionParams] = OMIT,
        response_format: typing.Optional[ResponseFormatParams] = OMIT,
        extra_body: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateChatCompletionResponse:
        """
        Creates a model response for the given chat conversation. OpenAI-compatible request and response shape; differences from OpenAI are called out in the Open-Source Models documentation.

        Requires `api-subscription-key`. Keys without beta access to this endpoint receive `400 invalid_request_error` before the model is called. Set `"stream": true` to receive the reply incrementally as server-sent events. Reasoning models return their chain-of-thought in a separate `reasoning_content` field on the message (`delta.reasoning_content` when streaming); reasoning tokens count against `max_tokens`.

        Parameters
        ----------
        messages : typing.Sequence[ChatCompletionRequestMessageParams]
            A list of messages comprising the conversation so far.

        model : V2ModelIds

        temperature : typing.Optional[float]
            What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.
            We generally recommend altering this or `top_p` but not both.

        top_p : typing.Optional[float]
            An alternative to sampling with temperature, called nucleus sampling,
            where the model considers the results of the tokens with top_p probability
            mass. So 0.1 means only the tokens comprising the top 10% probability mass
            are considered.

            We generally recommend altering this or `temperature` but not both.

        reasoning_effort : typing.Optional[ReasoningEffort]
            The effort to use for reasoning

        max_tokens : typing.Optional[int]
            The maximum number of tokens that can be generated in the chat completion.

        stream : typing.Optional[bool]
            If set to true, the model response data will be streamed to the client
            as it is generated using [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format).

        stop : typing.Optional[StopConfigurationParams]

        n : typing.Optional[int]
            How many chat completion choices to generate for each input message. Note that you will be charged based on the number of generated tokens across all of the choices. Keep `n` as `1` to minimize costs.

        seed : typing.Optional[int]
            This feature is in Beta.
            If specified, our system will make a best effort to sample deterministically, such that repeated requests with the same `seed` and parameters should return the same result.
            Determinism is not guaranteed, and you should refer to the `system_fingerprint` response parameter to monitor changes in the backend.

        frequency_penalty : typing.Optional[float]
            Number between -2.0 and 2.0. Positive values penalize new tokens based on
            their existing frequency in the text so far, decreasing the model's
            likelihood to repeat the same line verbatim.

        presence_penalty : typing.Optional[float]
            Number between -2.0 and 2.0. Positive values penalize new tokens based on
            whether they appear in the text so far, increasing the model's likelihood
            to talk about new topics.

        tools : typing.Optional[typing.Sequence[ChatCompletionToolParams]]
            A list of tools the model may call. Currently, only functions are supported as a tool.

        tool_choice : typing.Optional[ToolChoiceOptionParams]
            Controls which (if any) tool is called by the model.

        response_format : typing.Optional[ResponseFormatParams]
            An object specifying the format that the model must output. Setting `{ "type": "json_schema", "json_schema": {...} }` enables Structured Outputs which guarantees the model generates output matching the supplied JSON Schema. Setting `{ "type": "json_object" }` enables the older JSON mode, which guarantees valid JSON but not a specific schema.

        extra_body : typing.Optional[typing.Dict[str, typing.Any]]
            Extra body to be sent to the model.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateChatCompletionResponse
            Successful Response

        Examples
        --------
        import asyncio

        from sarvamai import AsyncSarvamAI

        client = AsyncSarvamAI(
            api_subscription_key="YOUR_API_SUBSCRIPTION_KEY",
        )


        async def main() -> None:
            await client.open_source_models.chat_completions_v2(
                messages=[
                    {
                        "role": "user",
                        "content": "Summarise the causes of the 2008 financial crisis.",
                    }
                ],
                model="glm5.2",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.chat_completions_v2(
            messages=messages,
            model=model,
            temperature=temperature,
            top_p=top_p,
            reasoning_effort=reasoning_effort,
            max_tokens=max_tokens,
            stream=stream,
            stop=stop,
            n=n,
            seed=seed,
            frequency_penalty=frequency_penalty,
            presence_penalty=presence_penalty,
            tools=tools,
            tool_choice=tool_choice,
            response_format=response_format,
            extra_body=extra_body,
            request_options=request_options,
        )
        return _response.data

    async def list_models_v2(self, *, request_options: typing.Optional[RequestOptions] = None) -> ModelListResponse:
        """
        Lists the model IDs this deployment currently serves. A model is only served where its backend is configured, so the list is environment-specific — fetch it rather than hardcoding IDs.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ModelListResponse
            Successful Response

        Examples
        --------
        import asyncio

        from sarvamai import AsyncSarvamAI

        client = AsyncSarvamAI(
            api_subscription_key="YOUR_API_SUBSCRIPTION_KEY",
        )


        async def main() -> None:
            await client.open_source_models.list_models_v2()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_models_v2(request_options=request_options)
        return _response.data

    async def get_model_v2(
        self, model_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ModelObject:
        """
        Retrieves one served model by ID.

        Parameters
        ----------
        model_id : str
            A model ID from `GET /v2/models`, e.g. `glm5.2`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ModelObject
            Successful Response

        Examples
        --------
        import asyncio

        from sarvamai import AsyncSarvamAI

        client = AsyncSarvamAI(
            api_subscription_key="YOUR_API_SUBSCRIPTION_KEY",
        )


        async def main() -> None:
            await client.open_source_models.get_model_v2(
                model_id="model_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_model_v2(model_id, request_options=request_options)
        return _response.data
